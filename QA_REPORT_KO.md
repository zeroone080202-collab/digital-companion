# MEDI v0.5.2 점검 결과

## 실제 의료 DB 확인

복원된 `data/knowledge.sqlite`에서 확인:

- 문서: 67,485
- 검색 조각: 200,492
- QA 조각: 15,359
- 참고문서 조각: 185,133

검색 테스트:

- `무릎이 너무 아파` → 5개 근거 검색
- `무릎이 붓고 계단 내려갈 때 아파` → 5개 근거 검색
- `허리가 아파` → 5개 근거 검색
- `상처가 붓고 열감이 있어요` → 5개 근거 검색

검증/heldout 자료는 실시간 검색 대상에 포함하지 않도록 기존 분리를 유지합니다.

## 코드 검사

- `app/config.py`, `app/provider.py`, `app/main.py`, `app/retrieval.py`, `app/schemas.py` Python 문법 검사 통과
- `static/app.js`, `static/local_ai.js` JavaScript 문법 검사 통과

## UI 브라우저 스모크 테스트

Chromium headless에서:

- 1440x980 데스크톱
- 390x844 모바일
- 라이트 모드 설정창
- 다크 모드 설정창
- 사용자 말풍선
- 첫 질문 주의창
- 의료 근거 표시
- 불필요한 `의료자료 검색` 공급자 흰 배지 제거
- 가로 overflow 점검

총 24개 V05 전용 검사 통과, page error 0건.

## 외부 AI 테스트 범위

실제 사용자의 Groq/Gemini 비밀키가 없으므로 외부 서비스에 실제 유료/무료 요청을 보내지는 않았습니다.
대신 공급자 응답을 모의한 통합 테스트로 `질문 → 실제 MEDI DB 검색 → 생성 답변 → 근거 표시` 흐름을 확인했습니다.
실제 배포 후에는 사용자가 만든 무료 API 키의 계정 한도 및 공급자 상태에 따라 동작합니다.

## 개인정보 보호 보강

외부 텍스트 AI로 보내기 전에 주민등록번호 형식, 휴대전화번호, 이메일 형식을 서버에서 마스킹하는 보조 필터를 추가했습니다. 이는 완전한 비식별화 시스템을 의미하지 않습니다.
